# The Odyssey — An Illustrated Glossary

## DONE: multi-plate Atlas expansion + the place→map plan + Phase 6 (all landed 2026-08-07)
Approved plan: `~/.claude/plans/functional-jumping-piglet.md`. Phases 1–3 and 5 are done (registry refactor, 5 new Ortelius plates tiled/uploaded/seeded, default flip + doc rewrite). **Phase 4 (Graecia calibration) applied 2026-08-07**: all 69 pins sit on Ortelius's own labels/symbols, placed by reading `plates/graecia/master.jpg` offline at native resolution (PIL grid-crops + a marked-ring verification pass over every pin — not the browser eyeball workflow; same `{term,x,y}` output). **Flagged-pin review RESOLVED 2026-08-07** (delegated by the user; re-run offline with fresh marked-ring crops): Chalcis/Crouni/Enipeus/Gyrae confirmed as drafted; **Phylace moved** (4530,4225)→(5080,4560) — Ortelius's printed "Phylace" is the Pierian homonym and the plate prints no Thessalian one, so geography follows Wilson's "city in Thessaly" def; **Argos↔Mycenae exchanged** — the engraved diagonal "Mycenę." anchors the SW castle and "Argos." sits beside the NE castle, so both pins now ride their own printed labels. **Phase 0 of the place-search→map plan (repo-root `Plan.md`) applied 2026-08-07**: rubri's 69 redundant Greek pins deleted (100 → 31), its Arabia/Persia/India placeholders calibrated, and aegyptus's Egypt/Pharos calibrated. (Correction: that round's "no pin work remains anywhere" claim was wrong — natoliae's and palestinae's 6 pins were still sitting on their original seed grids, caught and fixed 2026-08-07 while harvesting TODO 1b's Latin names below, since that work needed real coordinates to read labels from.) Run `npm run check:pins` after any pin change. **The repo-root `Plan.md` is now fully applied — every phase, including the once-parked Phase D (Journey-map routing for the mythic places, hero mini-map, "Voyage" card links), landed 2026-08-07** — so the old "next phase" resume protocol has nothing left to run; `Plan.md` remains as the record of what each phase built and the traps it hit. **Phase 6 is also done (2026-08-07)** — 7 poem-text places (Crete, Knossos, Thebes, Alpheus, Enipeus, Crouni, Achaea) promoted from `noGloss` pins to full glossary entries, verified against Wilson's actual verse; see TODO 7 below for the method and the two pin fixes it forced. **TODO 1b is also done (2026-08-07)** — every one of the 111 pins now carries a `latin` field or a documented reason it has none; see TODO 1b below for what shipped. **Open now:** the real-device mobile QA (TODO 2), plus optional polish (TODOs 3b/4/5/6).

A bilingual (English · 简体中文 · Pīnyīn) illustrated glossary of **167 entries** from Homer's
*Odyssey* (Emily Wilson translation, W. W. Norton) — incl. **91 places**: all 84 in Wilson's own
glossary, plus the 7 Phase 6 added (places her verse names but her glossary omits — see TODO 7). Paired with public-domain / openly-licensed artwork: **492 unique artworks** (deduped by
source AND by perceptual hash so no image repeats) plus **6 shared antique maps**.

- **Live:** https://tellmeohmuse.com — the canonical domain (Vercel project `odysseygloss`, auto-deploys on push to `main`). singohmuse.com, both www variants, and odysseygloss.vercel.app all 308-redirect to it (the vercel.app one via the host-matched redirect in `vercel.json`; preview URLs are unaffected). Sitemap/canonical/OG/JSON-LD URLs all use tellmeohmuse.com.
- **Repo:** github.com/benaustralia/odyssey
- **Final study PDF** (separate deliverable): `~/Desktop/Odyssey Student Packet (final).pdf`

## Stack & hard conventions
- **DaisyUI only** for UI (theme locked to `dracula`). No shadcn, no Radix, **no hand-written utility CSS for our own components** — use DaisyUI + Tailwind utilities (incl. responsive `sm:` breakpoints) only. `src/index.css` is the Tailwind import, DaisyUI plugin, font tokens, body font — **plus a few necessary vendor overrides**: YARL caption text-selection, Leaflet popup theming, and the `journey-route` marching-dash keyframes. Keep additions to vendor-override/animation only.
- **Image viewer:** `yet-another-react-lightbox` (YARL) with Thumbnails, Captions, Zoom, Fullscreen plugins. Do NOT rebuild a carousel by hand — that caused many bugs.
- **Interactive maps:** `leaflet` + `react-leaflet` + `leaflet-minimap` (overview navigator) power two features, both code-split & lazy-loaded so Leaflet stays out of the initial bundle: the "Journey of Odysseus" (`src/JourneyMap.tsx`, CRS.Simple over a single `ImageOverlay`) and the "Atlas" (`src/AtlasMap.tsx`, CRS.Simple over a Cloudflare R2-hosted `TileLayer` pyramid — see "Atlas map" below for why it needs tiles instead of one image).
- **Fonts:** `Cinzel` (serif) for the hero masthead ONLY; `Hanken Grotesk` (sans) everywhere else; `Noto Sans SC` for Chinese. Loaded async in `index.html`; Cinzel latin woff2 is preloaded.

## Data model
- `src/data/glossary.json` — array of entries: `{ term, pron, tag, def, zhName, zhPinyin, zhDef, art: string[] }`. `art` is an ordered list of keys into art.json; `art[0]` is the card cover.
- `src/data/art.json` — `{ key: { file, artist, title, year, source, license?, cld } }`. `file` (`/art/<key>.jpg`) is the actual delivery path — `assetUrl()` in `App.tsx` builds the R2 URL directly from it. `cld` is a leftover Cloudinary public_id from before the R2 migration; it's dead data now, kept only because stripping it from 574 entries isn't worth doing. `license` is only set for non-PD (CC) images.
- `categoryOf(tag)` maps tags → 4 filter categories: **gods** (god\*), **monsters** (monster), **mortals** (person/hero/people/animal), **world** (place/thing/idea/trick). Filter labels: Gods · Mortals · Monsters · **World**.

## Captions / licensing
- Caption (see `slides` in `App.tsx`): the credit (`Artist · Title, year`) and the optional `note` go in the YARL slide **`description`** (credit line, then `<br/>`, then note), and the slide **`title` is left empty on purpose** — YARL renders `title` in the top toolbar with `white-space:nowrap;text-overflow:ellipsis`, so it truncated on mobile. `description` wraps (set `descriptionMaxLines:5`). Graceful omission throughout.
- **Licence is NOT shown per-image** (was removed from the caption line) — the footer's generic "public domain or openly licensed (CC)" covers attribution. `license` stays in `art.json` for record only.
- **Metadata fill (done):** every non-map artwork now has an `artist` (real painter/vase-painter name, or `"Anonymous"` for anonymous ancient works) and a `year` (filled from each Commons source's *object* date — NEVER the photo/upload timestamp, which is the trap; circa dates where uncertain). `captions.csv` (repo root) is a one-row-per-image export for eyeballing.
- The `note` is a short subject locator/clarifier — locate the subject in a multi-figure work ("Ajax — the warrior at right (inscribed AIANTOS)") or explain an indirect-but-valid connection ("The Titans — Cronus's kin — cast down by the Olympians"). Written by viewing each image (vision pass); conservative (blank when single-figure/obvious/uncertain). **282 of 500** images have a `note`; **105** are CC-licensed. **English titles only** — foreign/messy/catalogue titles were stripped. **All** titles (harvested + originals) were standardised: translated to English, museum/inventory/accession noise removed, truncations/"artist-as-title"/book-scan prefixes fixed, Title Case; `artist` fields scrubbed of Commons uploader/photographer handles (kept real painters + vase-painter conventions; blanked anonymous ancient works). Re-standardise from the `source` filename (ground truth) — the stored `title` was sometimes lossy/truncated.
- **Places & maps:** all 91 places are `tag: "place"` → **World** filter. Each is illustrated by one of **6 antique PD maps** (Ortelius *Vlyssis Errores* / Wanderings of Ulysses & *Aegyptus Antiqua*, Lapie *Voyages*, *Graecia Homerica*, Delisle *Northern Greece*, *The World according to Homer*), `public/art/map-*.jpg`. A map is **shared across the many places it contains**: each place has its own art record keyed `<slug>-map` that reuses the map's `cld` but carries its own locating `note` ("Chios — an Aegean island off the coast of Asia Minor"). So maps are the ONE deliberate exception to image-uniqueness — exclude `*-map` keys from both the source-dedup and perceptual-dup checks.
- **Vlyssis Errores fix (done):** the `map-wanderings.jpg` ("Wanderings of Ulysses") file was originally **mislabelled** — it held Ortelius's *Red Sea* (Erythraean) plate, not the Mediterranean Ulysses map. Replaced with the **genuine *Vlyssis Errores* (1597) inset**, cropped from a 13238px PD scan of the Red Sea plate (the inset lives in its lower half; user hand-cropped the final framing). This same image is the **base of the Journey map** below. The 16 wandering-isle places (`*-map` keys sharing its `cld`) now show the correct map.
- **Perceptual-dup check (tool):** beyond source-filename dedup, the same artwork sometimes exists as two different files (low-res caption-baked vs clean high-res). Detect with a PIL dHash+aHash sweep over `public/art/*.jpg` (restrict to live art.json keys, skip `map-*`); pairs with dHash≤12 & aHash≤18 are candidates — VIEW a side-by-side montage to confirm (dark images throw false positives), keep the higher-res/caption-free copy. Re-run after big harvests.
- **Dedup invariant:** every NON-MAP image appears once. Enforced by grouping on the `source` Commons filename and keeping a single key per group, **preferring the one that is some entry's cover** so no card cover is orphaned. Re-run after any harvest. Note: deduped images may remain as orphan assets in the R2 bucket (see the orphan-cleanup note under "Image hosting" below).
- Footer says "public domain or openly licensed (CC)". Sourced from Wikimedia Commons. Accept PD / CC0 / CC-BY / CC-BY-SA only.

## Deploy
`npm run build` then `vercel --prod --yes` (or just `git push` — auto-deploys). Lighthouse target was 100/100/100/100 (achieved locally; production perf varies with network).

## TODO / open items
0. ~~**ACTIVE PLAN — place search → map deep-links: repo-root `Plan.md`**~~ **COMPLETE 2026-08-07 — all phases (0, A, B, C, D) applied and live.** Place cards and both map searches now deep-link to the right map focused on the place (`#atlas/<slug>/@<term>` / `#journey/<slug>/@<term>`) instead of dead-ending in a gallery showing a generic map JPEG; each place card shows its own cropped cover from the plate it links to; the voyage isles route to the Journey map via one shared policy (`src/data/mapRoutes.ts`). `Plan.md` stays as the record — per-phase "as built" notes and the traps each phase hit. Nothing in it is outstanding, so "next phase" has nothing left to run.
1. **Atlas pin follow-ups — ALL DONE, reminder retired** (see IN-FLIGHT note at top):
   (a) ~~spot-check the flagged Graecia pins~~ **DONE 2026-08-07** (review delegated to the assistant, re-run offline with marked-ring crops): Chalcis/Crouni/Enipeus/Gyrae confirmed; Phylace → Thessalian (5080,4560); Argos↔Mycenae swapped onto their own engraved labels;
   (b) ~~delete the now-redundant Greek pins from `rubri.ts` and recalibrate its Arabia/Persia/India placeholders~~ **DONE 2026-08-07** (Plan.md Phase 0): 69 Greek terms deleted (rubri 100 → 31 pins), Arabia/Persia/India calibrated onto their printed labels;
   (c) ~~`aegyptus.ts`'s 2 pins (Egypt, Pharos) parked in "AEGYPTIVM MARE"~~ **DONE 2026-08-07** (Plan.md Phase 0): Egypt on the "AEGYPTVS INFERIOR" Delta label, Pharos on "Pharos colonia" island inside the Alexandria-nome inset.
1b. ~~REQUESTED, NOT STARTED — show each place's engraved Latin name in brackets beside its English name~~ **DONE 2026-08-07.** A card/popup now reads "Crete *(CRETA)*", "Knossos *(Cnodos)*", "Thebes *(Thebę)*" — the toponym as Ortelius actually engraved it.
   - **Surfaces (per the user's decision):** all three — glossary card headings, Atlas pin popups, and the in-Atlas search rows (which also now match Latin spellings, so typing "Cnodos" finds Knossos). The bracket shows for `noGloss` pins too. `AtlasPlace` gained `latin?: string` (`src/data/plates/types.ts`); `latinFor()` in `src/data/plates/index.ts` is the one shared display helper — it hides the bracket when the engraved form is just the English name again case-insensitively (no "Persia (PERSIA)" stutter).
   - **Harvested all 111 pins** by reading each plate master at native resolution: 8 parallel reading agents, each cropping a marked ring at the pin and transcribing exactly what's engraved (spread/spaced letters joined into one word, V-for-U and ę/æ preserved, abbreviation stops kept, a bare terminal stop after a complete word dropped). ~68 pins have a Latin form; ~43 deliberately don't — the plate genuinely prints no name there (graecia's already-documented L/M "no label" pins, most of rubri's loosely-placed Vlyssis-inset isles, rubri's Phoenicia/Sidon placeholders, India's "Mambari regnum" compromise, Ethiopia's Prester John anchor). `scripts/check-pins.ts` now enforces this: every pin must carry `latin` or be named on its `NO_LATIN` allowlist, so a future pin with neither fails the build loudly instead of silently missing a bracket.
   - **Found natoliae's and palestinae's 6 pins were STILL on their original seed grids** (parked in open water/near the title cartouche) despite the intro note above once claiming "no pin work remains anywhere" — harvesting their Latin names needed real coordinates first, so they got calibrated as part of this pass: Troy → "TROIA", Tenedos → "Tenedo" (natoliae; Mimas and Mount Solyma print no label there and stay `noGloss`-adjacent with no `latin`); Phoenicia → "PHOENICIVM", Sidon → "SIDON" (palestinae).
   - **A few corrections to the "already known" values from the original ask:** Achaea's full engraved form is "ACHAIA PROPRIA" (not bare "ACHAIA"); Persia is "PERSIA" with no trailing stop; Egypt on rubri is the ligature "ÆGYPTVS" (distinct from aegyptus.ts's own "AEGYPTVS INFERIOR" — same word, different plate, different printed form). Two rubri captions turned out to be full phrases worth keeping verbatim: Scheria → "Phæacia, quę Scheria", Ogygia → "Ogygia Calypsonis habitaculum".
   - No cover-crop re-bake was needed for the newly-calibrated pins: Mount Solyma/Phoenicia/Sidon all have real (non-map) artwork as their card covers, not baked plate crops, so their `-map` art keys don't exist and `scripts/bake_place_crops.py` never touches them.
2. **Mobile QA on a real device** for the Atlas map (and re-check Journey map) — see the checklist under "Atlas map" above. Only simulator-tested so far.
3. **Journey map — next steps:** (a) ~~make the hero a small version of the map + add "Show on map" deep-links from the 14 journey-place cards~~ **DONE 2026-08-07** (Plan.md Phase D): the hero carries a clickable voyage-map crop, and every card the voyage visits has a "Voyage" deep link; (b) optional mobile pin-declutter for the tight western cluster **— still open**.
4. Optional Atlas polish: mobile-tune the minimap thumbnail size; a drag-declutter pass for dense pin clusters at low zoom.
5. A few minor-character entries still share scenes for lack of unique art.
6. Optional: masthead `ΟΔΥΣΣΕΙΑ` is redundant with "The Odyssey" (could become `ΟΜΗΡΟΥ` / be removed).
7. ~~**Atlas Phase 6:** glossary entries for poem-text-only places~~ **DONE 2026-08-07.** **7 new entries** (160 → 167; 84 → 91 places): **Crete, Knossos, Thebes, Alpheus, Enipeus, Crouni, Achaea** — all `tag: "place"`, each with a per-place crop of Graecia baked by `scripts/bake_place_crops.py` (the Phase C path, which supersedes this item's original "upload one 1600px copy of each plate" plan), and their pins un-flagged from `noGloss`.
   - **Verification method (reusable):** `pdftotext` over the user's local copy of Wilson at `~/Documents/doing/The Odyssey (Homer, Emily Wilson (translation))…pdf`, **segmented to the verse only** — the file is 537pp and its introduction, translator's note, endnotes and Wilson's own glossary all name places the poem never does, so raw counts over the whole PDF are worthless. The books are labelled `BOOK 1`..`BOOK 24` (the *second* occurrence of each is the body; the first is the contents page), and the verse runs from `BOOK 1` to the `GLOSSARY` heading. Cross-checked against the PD Greek (Perseus `tlg0012.tlg002`) and Butler 1900.
   - **The six "doubtful" regions were right to doubt: `Arcadia`, `Attica`, `Boeotia`, `Laconia`, `Peloponnese`, `Thessaly` occur ZERO times in Wilson's verse** (and zero in the Greek — they're post-Homeric/Ortelian regional names). They stay `noGloss`, as do Arabia/India/Persia/Africa. Don't revisit this without re-running the check.
   - Beware noisy Greek roots when re-checking: `ενιπ` also matches ἐνιπή "rebuke", `αθηναι` matches Ἀθηναίη (Athena), `κνωσ` matches κνώσσω "sleep", and an English `\bAchaea` prefix matches every "Achaeans".
   - **Achaea is the judgment call**: it appears once (Book 21, Ἀχαιΐδα γαῖαν "the Achaean land") meaning Greece at large, while its pin rides Ortelius's printed ACHAIA, the small northern-Peloponnese region. Included, with the def stating both senses; the neighbouring `Achaeans` entry (people) stays separate. Reversible if it reads as redundant.
   - **Pin fixes this forced (same reasoning as Phase C's africae catch):** promoting a region pin to a city entry changed what "correct placement" means. **Thebes** (6315,5450)→**(6440,5403)**, off the "BOEOTIA" region lettering and onto Ortelius's own engraved "Thebę" beside its walled-city symbol; **Crouni** (3985,6870)→**(4070,6885)**, off an unnamed offshore islet onto the Cyparissian shore beside Chalcis. Verified with marked-ring crops at native resolution ([[graecia-pin-drafting-method]]).
   - **Interpretive placements to be honest about:** this plate prints no "Enipeus" and no "Crouni" (nor "Chalcis"), so those three pins are locators, not label matches — the same class of compromise as rubri's India and africae's Ethiopia. Crete/Knossos/Thebes/Alpheus/Achaea all sit on their own engraved toponyms ("CRETA", "Cnodos", "Thebę", "Alpheus flu.", "ACHAIA").

**Done (context): Aug 2026 — migrated off Cloudinary to Cloudflare R2** (see "Image hosting" and "Atlas map" above for the full setup): Cloudinary's shared cross-project account was disabled for billing overage and permanently deleted; all 512 art masters and ~11,650 Atlas tiles (incl. regenerating `rubri`'s pyramid, whose tiles had never been persisted locally) re-uploaded to a new R2 bucket via boto3/S3-compatible endpoint (the plain REST API hit an account-wide rate limit at this volume); `App.tsx`, `AtlasMap.tsx`, all 6 plate configs, and the Journey map's own base-image reference rewired to R2 URLs; minimap thumbnails (previously a Cloudinary `c_crop`) pre-baked per plate since R2 has no live transform API. **2026-08-11:** the bucket (`odyssey-assets`) got GET/HEAD CORS (`AllowedOrigins: *`, via S3 `put_bucket_cors` with the `.env.r2.local` creds) for the lightbox's Download button, whose blob fetch needs `Access-Control-Allow-Origin`. Trap: slides pass a `?download` cache-buster as `downloadUrl` because the browser caches the plain `<img>` response WITHOUT CORS headers (R2 sends no `Vary` on Origin-less requests), and reusing that cached entry for the download's CORS fetch fails silently → the plugin degrades to open-in-new-tab.

every-image-appears-once dedup (Hayez/Pinturicchio/Kleophrades over-use removed; method: pull from the entry's Wikipedia article via `generator=images`, filter PD/CC); curated additions (June 2026: Burne-Jones *Wine of Circe*, Primaticcio *Ulysses and Penelope*, Esquiline Underworld panels, Sperlonga *Scylla Group*, an *L'Odissea* 1911 PD film frame → Cyclops — modern stills stay excluded); the caption/metadata fill, *Vlyssis Errores* fix, the interactive Journey map, and the tiled Atlas map of the full Red Sea plate with Egypt/Ethiopia pins. **July 2026:** added a third Atlas pin (Pharos, off Alexandria); widened the Atlas pin hit target to 40px + `cursor-grab`; extracted the two maps' floating calibration panel into shared `src/CalibrationPanel.tsx`; fixed a corner-clip bug where the map's `overflow-hidden rounded-box` wrapper sliced the panel's own top-left corner; made the calibration panel draggable anywhere in the viewport via a `document.body` portal; populated the atlas map with pins for the glossary places; improved calibration panel UX with top-right default on desktop and collapse toggle on mobile; restored native Leaflet pin dragging in `#atlas-eyeball` after a six-commit hand-rolled failure spiral; **generalized `JourneyMap` for multiple personages** (`src/data/journeys/` config registry, `#journey/<slug>(-eyeball)` routing — the "planned" TODO for this is done); **multi-plate Atlas expansion** (Phases 1–3, 5): registry refactor mirroring `src/data/journeys/`, 5 new Ortelius plates (Graecia, Aegyptus, Natoliae, Palestinae, Africae) tiled/uploaded/seeded via `scripts/make-plate.sh` + `scripts/upload_tiles.py`, `scripts/check-pins.ts` data guard, `DEFAULT_PLATE_SLUG` flipped to `"graecia"` — Phase 4's hands-on pin calibration landed 2026-08-07 and its flag review was resolved the same day (item 1 above).

## Sister project
`~/Documents/smell/web` → Vercel project `smellcombo`, live at https://smellcombo.vercel.app (Next.js; needs `vercel.json` with `{"framework":"nextjs"}`).
