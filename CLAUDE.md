# The Odyssey — An Illustrated Glossary

## IN-FLIGHT: multi-plate Atlas expansion — ALL pin work DONE (flagged-pin review resolved 2026-08-07)
Approved plan: `~/.claude/plans/functional-jumping-piglet.md`. Phases 1–3 and 5 are done (registry refactor, 5 new Ortelius plates tiled/uploaded/seeded, default flip + doc rewrite). **Phase 4 (Graecia calibration) applied 2026-08-07**: all 69 pins sit on Ortelius's own labels/symbols, placed by reading `plates/graecia/master.jpg` offline at native resolution (PIL grid-crops + a marked-ring verification pass over every pin — not the browser eyeball workflow; same `{term,x,y}` output). Per-pin confidence + evidence lives in `graecia-draft/flags.md` (untracked, alongside marked review PNGs). **Flagged-pin review RESOLVED 2026-08-07** (delegated by the user; re-run offline with fresh marked-ring crops — see flags.md "Review resolution" + `graecia-draft/verify/`): Chalcis/Crouni/Enipeus/Gyrae confirmed as drafted; **Phylace moved** (4530,4225)→(5080,4560) — Ortelius's printed "Phylace" is the Pierian homonym and the plate prints no Thessalian one, so geography follows Wilson's "city in Thessaly" def; **Argos↔Mycenae exchanged** — the engraved diagonal "Mycenę." anchors the SW castle and "Argos." sits beside the NE castle, so both pins now ride their own printed labels. **Phase 0 of the place-search→map plan (repo-root `Plan.md`) applied 2026-08-07**: rubri's 69 redundant Greek pins deleted (100 → 31), its Arabia/Persia/India placeholders calibrated, and aegyptus's Egypt/Pharos calibrated — so with the flag review above also resolved, **no pin work remains anywhere**. Run `npm run check:pins` after any pin change (resume protocol: the user `/clear`s, then prompts "next phase" → read `Plan.md`, run its first unchecked phase only). Phase 6 (glossary entries for poem-text-only places — see below) is opted into but not started.

A bilingual (English · 简体中文 · Pīnyīn) illustrated glossary of **160 entries** from Homer's
*Odyssey* (Emily Wilson translation, W. W. Norton) — incl. **84 places** (every place in Wilson's
glossary). Paired with public-domain / openly-licensed artwork: **492 unique artworks** (deduped by
source AND by perceptual hash so no image repeats) plus **6 shared antique maps**.

- **Live:** https://odysseygloss.vercel.app (Vercel project `odysseygloss`, native domain, auto-deploys on push to `main`)
- **Repo:** github.com/benaustralia/odyssey
- **Final study PDF** (separate deliverable): `~/Desktop/Odyssey Student Packet (final).pdf`

## Stack & hard conventions
- **React + Vite + TypeScript.**
- **DaisyUI only** for UI (theme locked to `dracula`). No shadcn, no Radix, **no hand-written utility CSS for our own components** — use DaisyUI + Tailwind utilities (incl. responsive `sm:` breakpoints) only. `src/index.css` is the Tailwind import, DaisyUI plugin, font tokens, body font — **plus a few necessary vendor overrides**: YARL caption text-selection, Leaflet popup theming, and the `journey-route` marching-dash keyframes. Keep additions to vendor-override/animation only.
- **Image viewer:** `yet-another-react-lightbox` (YARL) with Thumbnails, Captions, Zoom, Fullscreen plugins. Do NOT rebuild a carousel by hand — that caused many bugs.
- **Interactive maps:** `leaflet` + `react-leaflet` + `leaflet-minimap` (overview navigator) power two features, both code-split & lazy-loaded so Leaflet stays out of the initial bundle: the "Journey of Odysseus" (`src/JourneyMap.tsx`, CRS.Simple over a single `ImageOverlay`) and the "Atlas" (`src/AtlasMap.tsx`, CRS.Simple over a Cloudflare R2-hosted `TileLayer` pyramid — see "Atlas map" below for why it needs tiles instead of one image).
- **Fonts:** `Cinzel` (serif) for the hero masthead ONLY; `Hanken Grotesk` (sans) everywhere else; `Noto Sans SC` for Chinese. Loaded async in `index.html`; Cinzel latin woff2 is preloaded.
- Most logic lives in `src/App.tsx`; the two interactive maps are `src/JourneyMap.tsx` and `src/AtlasMap.tsx`.

## Data model
- `src/data/glossary.json` — array of entries: `{ term, pron, tag, def, zhName, zhPinyin, zhDef, art: string[] }`. `art` is an ordered list of keys into art.json; `art[0]` is the card cover.
- `src/data/art.json` — `{ key: { file, artist, title, year, source, license?, cld } }`. `file` (`/art/<key>.jpg`) is the actual delivery path — `assetUrl()` in `App.tsx` builds the R2 URL directly from it. `cld` is a leftover Cloudinary public_id from before the R2 migration; it's dead data now, kept only because stripping it from 574 entries isn't worth doing. `license` is only set for non-PD (CC) images.
- `categoryOf(tag)` maps tags → 4 filter categories: **gods** (god\*), **monsters** (monster), **mortals** (person/hero/people/animal), **world** (place/thing/idea/trick). Filter labels: Gods · Mortals · Monsters · **World**.

## Captions / licensing
- Caption (see `slides` in `App.tsx`): the credit (`Artist · Title, year`) and the optional `note` go in the YARL slide **`description`** (credit line, then `<br/>`, then note), and the slide **`title` is left empty on purpose** — YARL renders `title` in the top toolbar with `white-space:nowrap;text-overflow:ellipsis`, so it truncated on mobile. `description` wraps (set `descriptionMaxLines:5`). Graceful omission throughout.
- **Licence is NOT shown per-image** (was removed from the caption line) — the footer's generic "public domain or openly licensed (CC)" covers attribution. `license` stays in `art.json` for record only.
- **Metadata fill (done):** every non-map artwork now has an `artist` (real painter/vase-painter name, or `"Anonymous"` for anonymous ancient works) and a `year` (filled from each Commons source's *object* date — NEVER the photo/upload timestamp, which is the trap; circa dates where uncertain). `captions.csv` (repo root) is a one-row-per-image export for eyeballing.
- The `note` is a short subject locator/clarifier — locate the subject in a multi-figure work ("Ajax — the warrior at right (inscribed AIANTOS)") or explain an indirect-but-valid connection ("The Titans — Cronus's kin — cast down by the Olympians"). Written by viewing each image (vision pass); conservative (blank when single-figure/obvious/uncertain). **282 of 500** images have a `note`; **105** are CC-licensed. **English titles only** — foreign/messy/catalogue titles were stripped. **All** titles (harvested + originals) were standardised: translated to English, museum/inventory/accession noise removed, truncations/"artist-as-title"/book-scan prefixes fixed, Title Case; `artist` fields scrubbed of Commons uploader/photographer handles (kept real painters + vase-painter conventions; blanked anonymous ancient works). Re-standardise from the `source` filename (ground truth) — the stored `title` was sometimes lossy/truncated.
- **Places & maps:** all 84 places (from Wilson's glossary) are `tag: "place"` → **World** filter. Each is illustrated by one of **6 antique PD maps** (Ortelius *Vlyssis Errores* / Wanderings of Ulysses & *Aegyptus Antiqua*, Lapie *Voyages*, *Graecia Homerica*, Delisle *Northern Greece*, *The World according to Homer*), `public/art/map-*.jpg`. A map is **shared across the many places it contains**: each place has its own art record keyed `<slug>-map` that reuses the map's `cld` but carries its own locating `note` ("Chios — an Aegean island off the coast of Asia Minor"). So maps are the ONE deliberate exception to image-uniqueness — exclude `*-map` keys from both the source-dedup and perceptual-dup checks.
- **Vlyssis Errores fix (done):** the `map-wanderings.jpg` ("Wanderings of Ulysses") file was originally **mislabelled** — it held Ortelius's *Red Sea* (Erythraean) plate, not the Mediterranean Ulysses map. Replaced with the **genuine *Vlyssis Errores* (1597) inset**, cropped from a 13238px PD scan of the Red Sea plate (the inset lives in its lower half; user hand-cropped the final framing). This same image is the **base of the Journey map** below. The 16 wandering-isle places (`*-map` keys sharing its `cld`) now show the correct map.
- **Perceptual-dup check (tool):** beyond source-filename dedup, the same artwork sometimes exists as two different files (low-res caption-baked vs clean high-res). Detect with a PIL dHash+aHash sweep over `public/art/*.jpg` (restrict to live art.json keys, skip `map-*`); pairs with dHash≤12 & aHash≤18 are candidates — VIEW a side-by-side montage to confirm (dark images throw false positives), keep the higher-res/caption-free copy. Re-run after big harvests.
- **Dedup invariant:** every NON-MAP image appears once. Enforced by grouping on the `source` Commons filename and keeping a single key per group, **preferring the one that is some entry's cover** so no card cover is orphaned. Re-run after any harvest. Note: deduped images may remain as orphan assets in the R2 bucket (see the orphan-cleanup note under "Image hosting" below).
- Footer says "public domain or openly licensed (CC)". Sourced from Wikimedia Commons. Accept PD / CC0 / CC-BY / CC-BY-SA only.

## Working with artwork (lessons learned)
- Fetch scripts query the Commons API (`generator=search`, `prop=imageinfo`, `iiprop=url|mime|extmetadata`). **Filter aggressively:** skip `.pdf/.djvu/.svg`, book scans (`(IA `, "Adventures of Ulysses"), maps, logos, and species/astronomy named after Greek figures (butterflies like *Morpho menelaus* / *Eumaeus atala*, planet "Saturn with auroras"), film stills, perfumery signs, "goddess card" composites.
- After fetching: build a contact-sheet montage with ImageMagick and eyeball it; check for cross-language duplicate works (same painting filed under English + French/German/Dutch names — dedupe, keep English).
- A greedy "cover diversification" pass reorders each entry's `art` so covers are unique where alternatives exist.
- **Full-article harvest (done):** pulled *every* PD/CC image off each entry's en.wikipedia article (`generator=images`), keyed `<slug>-w<NN>`. This sweeps in junk that `generator=images` can't pre-filter — maps, coins, site/building/landscape photos, Britannica diagrams, book-plate scans, museum replicas, ritual photos, a fossil skull, and **off-subject gallery/navbox thumbnails** (e.g. Bernini's *Apollo & Daphne* landed on Ajax). The original Commons filename lives in the `source` field (the on-disk `file` is renamed) — QA by dumping `source` basenames + a contact-sheet montage, then prune. 32 were pruned this round.
- **Image weight:** local masters in `public/art` are recompressed to `-resize '1600x1600>' -quality 82 -interlace Plane -sampling-factor 4:2:0 -strip` before upload.
- **Relevance + caption audit (done):** a vision pass over *every* image (subagents that actually open each `public/art/<key>.jpg`) checking (a) is it on-subject for its entry, (b) is the caption helpful. `generator=images` pulls tangential article images that *look* fine by filename but are off-subject — e.g. Blake's *Comus* on Circe, *School of Athens* on hospitality, a "Marlowe" portrait on Helen, a Theseus/Minotaur cup on Hades. **Removed off-subject; never trust the title alone — verify by viewing.** Also killed unusable images (dense scholarly "Nekyia reconstruction" you can't zoom) and same-scene duplicates (kept the clearer one — e.g. Lykaon Painter vase over van Thulden's *Burning of Elpenor*). Be conservative: keep if it plausibly relates; a clarifying `note` often beats removal for indirect connections.
- **Ajax is two people:** "Ajax the Great" (Telamonian; Book 11 underworld shade) and "Ajax the Lesser" (Locrian, son of Oïleus; Book 4 drowning, the Cassandra outrage) are separate entries. Don't mix their art.

## Journey of Odysseus map (`src/JourneyMap.tsx`)
- Leaflet **CRS.Simple** image map over the *Vlyssis Errores* base — served as-is from R2 at `art/map-wanderings.jpg` (same object as the glossary's wandering-isle place entries; native 4000×2337, no resize needed since R2 has no on-the-fly transform, see "Image hosting" below). Coordinate space = the master's pixel size; `const W=4000, H=2337`. `yx(x,y)=[H-y,x]` flips y for CRS.Simple. **Note:** the old Cloudinary setup version-pinned this URL (`/v<ver>/<public_id>`) specifically to bust browser cache on re-upload; that mechanism doesn't exist for a flat R2 key — if this master ever needs replacing in place, add a cache-busting query param or use a new key, don't just overwrite silently (same caveat as the general R2 "swap an image" note below).
- **15 voyage stops** (`STOPS`, 14 canonical per Stephen Fry's "Journey of Odysseus" plus one extra) hand-calibrated onto Ortelius's own labels (Troy/Ilium, Cicones/Ismarus, Aeaea/Circe, Cimmerii/Hades, etc.). Each links to its glossary `term`. The extra stop is Odysseus's second landing on Aeaea (Book 12: he actually beaches the ship and stays the night to bury Elpenor and get Circe's final sailing directions, not just sail past) — it reuses `term: "Circe"` (same island, same glossary entry/artworks) so it needed its own React `key` (`s.n`, not `s.term`, since two stops now share a term).
- **Narrative sea route** in `LEG_VIAS[i]` (per-leg bend points) — faithful to Homer: round Cape Malea to the Lotus-Eaters, the western-Ocean dip to Hades and **back to Aeaea** (Circe visited twice), the **Scylla/Charybdis strait threaded twice** (out + the wreck-driven double-back). Smoothed with **centripetal Catmull-Rom** (`smooth()` — centripetal, not uniform, to avoid overshoot on uneven spacing; emits `ROUTE_SEG` points per control-segment, so input point `k` lands at smoothed index `k*ROUTE_SEG`). Drawn as two polylines: a white **casing** under the purple. **Gotcha:** the casing must use the **same `journey-route` dash+animation** as the purple — a *solid* casing fills the dash gaps and kills the marching-ants effect.
- Features: guided **tour**, zoom-aware place-name labels, hover tooltips, **click-popup** with bilingual entry info + "View artworks" (`lookup` prop from App resolves term→entry; the map stays open under the lightbox so closing returns to it), an overview **navigator** (`leaflet-minimap`), and a Fry-style legend.
- **Tour camera** (`TourController` + `panAlong`): between *adjacent* stops the camera **glides ALONG the route curve at a fixed `TOUR_ZOOM`** (rAF tween over the curve slice `routeLatLng[stopRouteIdx[from]..stopRouteIdx[to]]`, speed scaled to leg pixel-length, clamped) — NOT `flyTo` (which zooms out-then-in and feels disconnected). Non-adjacent jumps and the first stop ease straight in; idle (`idx<0`) zooms out to the whole map. Glide pace is by **on-screen pixels** (`glideMs(px)` = `TOUR_PPS` 150 px/s, clamped `GLIDE_MIN`/`GLIDE_MAX` 3.5–11s) — a sail, not a whip. **Cadence is decoupled, not a fixed `STOP_MS`:** each stop holds for `cycleMs = glide-in + DWELL_MS` (3.5s), so the huge Cicones→Lotus-Eaters leg (~6800px screen, vs ~700px typical) can glide its full ~11s without starving the dwell on short stops. `legGlideMs(from,to)` measures the route slice the same way `panAlong` does (px·2^TOUR_ZOOM), so timer + camera agree; both the auto-advance `setTimeout` and the **countdown bar** (`tour-progress` keyframe, `key`'d per stop) use `cycleMs`. A tiny antique-galley **ship** (`shipIcon` divIcon) rides the route during the tour — `panAlong`'s `onMove` callback `setLatLng`s it each frame; since the camera centers on that same point it sits at screen-center while the map scrolls (hidden via `opacity:0` when idle). Legend clicks drive a separate `focus` state that pans the camera through the same `TourController` while **keeping the legend open** — they don't start the tour or open artwork.
- **URL route:** `#journey` opens the map (App reads `window.location.hash`); `#journey/edit` (legacy alias: `#humaneyeball`) opens it in **calibration mode** — drag pins, click the route to add a sea-bend, drag/dbl-click bends, then "Copy coordinates" to get the `{stops, legVias}` JSON to paste back into the source.
- **Mobile:** modal goes full-screen, the wide map **covers** the screen (min zoom = `getBoundsZoom(bounds, true)`) and pans horizontally (follows the east→west voyage); opens focused on Troy; legend hidden, tour controls live in the tour card.
- **Calibration workflow:** to reposition pins/route, open `#journey/edit`, drag, copy the JSON, and bake values into `STOPS` / `LEG_VIAS` (don't hand-guess coordinates). To locate a label on the map, crop the master with PIL + a coordinate grid and view it. The floating "Calibration" panel (live JSON dump + Copy button) is the shared `src/CalibrationPanel.tsx` — see its own section below.
- **react-leaflet `pathOptions.className` is dropped in the PRODUCTION build (works in dev) — a real trap.** `createPathComponent` calls `new L.Polyline(positions, options)` with `className` nested inside `pathOptions`, but Leaflet only reads a *top-level* `options.className` in `_initPath`, and react-leaflet applies pathOptions via `setStyle()` which **never touches className**. So the class is never put on the SVG path. It *appears* to work in `vite` dev ONLY because StrictMode's double-mount re-runs `_initPath` after `setStyle` happened to merge className into the options — a fluke absent from the single-mount prod bundle. Symptom this session: the `journey-route` marching-ants class (hence the animation) vanished in production while fine on localhost. **Fix: apply the class yourself on the layer `add` event** (`eventHandlers={{ add: e => e.target.getElement()?.classList.add("journey-route") }}` → `markRoute`), not via `pathOptions.className`.
- **Dev/testing gotchas:** ports **5173/5174 are usually taken** by sibling projects (e.g. *Chord Drill*) — the dev server lands on a higher port; check the Vite log for the real one. **Dev≠prod for the map:** Vite serves CSS unminified and React in StrictMode, so the className bug above and any minifier CSS rewrite hide in dev — reproduce production issues with `npm run build` + `vite preview`. The `chrome-devtools` MCP refuses to attach if a Chrome with its profile is already open; fall back to driving the live/preview URL with **Playwright WebKit** (Safari engine — the iPhone test target) via the npx-cached `playwright` (scripts must live in that dir to resolve the module). When testing the **tour/animated state**, same-URL `#journey` navigation does **not** reset React state, and multiple open tabs each run their own timers — both cause misleading "advancing too fast" readings. Force a fresh mount (navigate to `/` then `#journey`, or a new tab) and verify intervals with `console.log(performance.now())`. Cadence is **per-leg `cycleMs`** (glide-in + `DWELL_MS`), not a single fixed value.

## Atlas map (`src/AtlasMap.tsx`)
- **Config-driven, multi-plate** (mirrors `JourneyMap`'s `src/data/journeys/` pattern): `src/data/plates/{types,index,<slug>}.ts` is the registry — `types.ts` defines `AtlasPlace` (`term`/`label?`/`noGloss?`/`x`/`y`) and `PlateConfig` (`slug`/`title`/`w`/`h`/`maxZoom`/`tileBase`/`attribution`/`places`); `index.ts` exports `PLATES: Record<string, PlateConfig>` + `DEFAULT_PLATE_SLUG` (currently `"graecia"`). `AtlasMap.tsx` itself takes `config` + `editing` props (no module-level constants) and renders a `<select>` plate switcher in the modal header.
- **6 plates**, all Ortelius (Theatrum Orbis Terrarum): `rubri` (*Erythraei Sive Rubri Maris Periplus*, 1597 — the original full-Red-Sea plate: Egypt/Arabia/Persia/India/East Africa plus the cramped Aegean *Vlyssis Errores* inset that the Journey map also uses; 13238×10802px, maxZoom 6, ~3010 tiles), `graecia` (*Graecia Sophiani* — the primary Greek map, mainland Greece/Peloponnese/Crete/Aegean at real cartographic scale; 12325×8913, maxZoom 6, ~2334 tiles), `aegyptus` (*Aegyptus Antiqua*, 6498×9896, maxZoom 6, ~1373 tiles), `natoliae` (*Natoliae... Asia Minor*, 5560×7725, maxZoom 5, ~923 tiles), `palestinae` (*Palestinae sive... Terrae Promissionis*, 8000×6144, maxZoom 5, ~1025 tiles), `africae` (*Africae Tabula Nova*, 12504×9430, maxZoom 6, ~2470 tiles). Masters/tiles live in git-ignored `plates/<slug>/` (not `public/art/` — a separate directory since the artwork and Atlas upload passes were built independently, though both now go through `scripts/upload_to_r2.py`). **Gotcha:** `rubri`'s tile pyramid predates the `plates/<slug>/tiles/` convention and was never actually persisted locally — only `plates/rubri/master.jpg` survived. It had to be regenerated (`vips dzsave` as above) during the Cloudinary→R2 migration; if any other plate's `tiles/` dir is ever found missing, the fix is the same — the master is the durable source of truth, tiles are always reproducible from it.
- **Why tile pyramids, not `ImageOverlay`s like JourneyMap:** each master is 60–140MP, far past what any live crop/transform endpoint could crop in one request — doubly true now: R2 has **no on-the-fly transform API at all** (Cloudinary at least had `c_crop`; R2 serves flat objects only), so pre-slicing offline is the only option, not just the fastest one. Sliced via `vips dzsave <master> tiles --layout google --suffix '.jpg[Q=82]' --tile-size 256 --overlap 0` (via `scripts/make-plate.sh <slug> "<Commons file title>"`, which also curls the master from `https://commons.wikimedia.org/wiki/Special:FilePath/<title>`).
- **Hosting:** each tile uploaded to Cloudflare **R2** (bucket `odyssey-assets`, public `pub-b57180e24c9841f58854ecd1c164523a.r2.dev` domain) at a **deterministic** object key, uniform across all 6 plates: `atlas/<slug>/{z}/{row}/{col}.jpg` (`config.tileBase` picks the `atlas/<slug>` prefix; the pre-namespacing legacy bare-`atlas` exception rubri had on Cloudinary was NOT carried over — R2 is a fresh key space). Uploaded via `scripts/upload_to_r2.py` (boto3 against R2's S3-compatible endpoint — see "Image hosting" below for why, and for credentials). Deterministic keys mean tile URLs need **no lookup manifest**: `${R2}/atlas/<slug>/{z}/{y}/{x}.jpg` — note the literal `.jpg` suffix per object (unlike Cloudinary's content-negotiated delivery, R2 objects were uploaded with explicit extensions).
- **Minimap thumbnails have no live-transform equivalent either:** the old `thumbUrl()` did a Cloudinary `c_crop` to pull just the plate's real content bounds out of the z0 tile's white-padded 256×256 canvas (see the padding note below). R2 can't do that on request, so each plate's crop is pre-baked *once* at upload time into a dedicated `atlas/<slug>/thumb.jpg` object (crop math: `ceil(w/2^maxZoom)+2 × ceil(h/2^maxZoom)+2` from `(0,0)` on the local `tiles/0/0/0.jpg`, then upload). Re-run this by hand (a short PIL + boto3 script, not currently a checked-in tool) any time a plate's tiles are regenerated.
- **Gotcha — tile addressing is TRANSPOSED:** `vips dzsave`'s "google" layout writes tiles on disk as `{z}/{row}/{col}.jpg` — row is the directory, col is the filename — the transpose of Leaflet's `{z}/{x}/{y}` template (x=column, y=row). Get this backwards and the map renders as a **scrambled, transposed puzzle** (each tile intact, wrong tile in each cell) — NOT a blank/missing-tile failure, which makes it a confusing bug to spot. Fix: swap y/x in the URL template (`atlas/{z}/{y}/{x}`, not `atlas/{z}/{x}/{y}`).
- **Gotcha — `L.CRS.Simple.unproject(point, zoom)` called STATICALLY silently skips the scale division** (it just applies the axis-flip transform to whatever raw point you give it) — only the LIVE MAP INSTANCE's `map.unproject(point, zoom)` correctly divides by `scale(zoom)=2^zoom` first. Bounds/marker math built from the static call end up in raw pixel units, and `fitBounds` "solves" the resulting nonsense box by landing on a wrong zoom, which makes the tile grid compute negative row/col indices (a wall of 404s like `atlas/2/-2/1`). **Always compute bounds/positions via the live map's instance methods** (`unprojectPixel(map, x, y, maxZoom)` helper in `AtlasMap.tsx`), never the static `L.CRS.Simple` object.
- **Modal-mount timing:** react-leaflet's initial `bounds`-prop fit runs before the modal's CSS layout has settled (container still 0×0) — fixed with a `ResizeObserver` (`FitWhenReady`) that fits once the container has real dimensions. Must fit **only once** (guarded by a `fitted` flag) — re-running `fitBounds` on every observed resize (Leaflet's own zoom-animation causes micro-reflows the observer also sees) snapped the view back to "fit all" on every scroll-zoom tick, making scroll-to-zoom appear completely broken. Same one-shot idea as `JourneyMap`'s `LockMinZoom` (`focused` flag). Switching plates remounts `AtlasMap` via `key={slug}` so this and tile-prefetch state reset cleanly.
- **Mobile "cover" fit:** same fix as `JourneyMap`'s `LockMinZoom` — `map.getBoundsZoom(bounds, true)` as the min zoom, so the map always fills the frame instead of "fit inside" leaving grey letterbox bands on a portrait phone (caught only by testing on an actual iPhone SE simulator, not visible on desktop where the container is aspect-locked to the image via `sm:aspect-[...]`).
- **Calibration:** `#atlas/edit` (default plate) or `#atlas/<slug>/edit` (any plate) — Shift+click the map to drop a new pin (named "untitled", rename in the footer panel), **drag existing pins** to adjust (`dragend` converts the marker's `getLatLng()` back to pixel coords via `map.project()`), then "Copy all" in the `CalibrationFooter` (search → crosshair-focus → drag → copy) to get the plate's JSON to paste back into its `src/data/plates/<slug>.ts` file. The footer's crosshair button, besides centering, makes its pin announce itself — swaps to `pinIconFocused` (warning-yellow dot + looping `animate-ping` halo, `zIndexOffset` above neighbours) for 8s, so the target is findable among dozens of identical dots. **Pin drag must stay Leaflet-NATIVE (`draggable` prop + `dragend`)** — a hand-rolled rewrite cost 6 failed commits: Leaflet markers never fire `pointerdown`/`pointermove` (Leaflet translates pointer events to `mousedown`/`mousemove` before dispatch), and "Shift+drag the pin" can't work either (Shift+drag is Leaflet's BoxZoom gesture at the container level, and `L.Draggable` ignores shift-mousedowns anyway — also no Shift key on iOS). Pin-drop requires **Shift+click** (plain clicks — including both clicks of a double-click-to-zoom — used to litter stray "untitled" pins); that makes adding pins desktop-only, since iOS has no Shift key.
- **Pin hit target:** the visible dot is 24px (`size-6`), but `pinIcon`'s actual `divIcon` is 40px (`iconSize:[40,40]`, dot centered inside via a padded wrapper) with `cursor-grab`/`active:cursor-grabbing` — a slightly imprecise mousedown still grabs the marker instead of falling through to the map's own click handler, which in eyeball mode means "drop a stray new pin", not "drag this one".
- **Positional marker keys + click-reorder = dead popups (fixed 2026-07-17):** pin markers are keyed `` `${term}-${i}` `` and the calibration footer's float-to-top reorders `pins` on marker click — while that reorder ran in VIEW mode too, the clicked marker's key shifted, React remounted it, and Leaflet closes a popup when its marker is removed, so every popup died ~200ms after opening and "View artworks" was unreachable from ALL atlas pins in production (confirmed live via a MutationObserver: popup-added → popup-removed 206ms). `onClickPin` is now passed **only in eyeball mode**. If pin popups ever die-on-open again, suspect any `setPins` (or key-affecting state) in a click path.
- **Known-benign console noise:** ~4× `…/{z}/{row}/-1` tile 404s when a plate opens — Leaflet's `keepBuffer` requests one column past the plate's left edge in the tick before `FitWhenReady`'s `bounds` lands on the `TileLayer` prop. Cosmetic only (fires on the wider plates: rubri/graecia/aegyptus/africae). All in-range tiles exist — every pyramid sampled to its max-zoom corners (originally on Cloudinary 2026-07-17, re-confirmed on R2 2026-08-04 post-migration), incl. the just-past-corner 404 proving config dims match pyramid extents.
- **Pins:** `term` must exactly match the glossary term (or carry `noGloss: true` for deliberate poem-text/label-only pins) or the popup's "View artworks" lookup silently fails (bit us twice: "Mount Olympus" vs glossary "Olympus"; a whole batch-rename in commit `2c4afef`). `label?` supplies a shorter display name without touching the lookup key (e.g. `term: "Argos (the city)"`, `label: "Argos"`, to disambiguate from the dog's entry). **`npm run check:pins`** (`scripts/check-pins.ts`, tsx) guards term validity, plate pixel bounds, per-plate duplicate terms, journey-stop terms, and glossary place coverage (only `Ocean` is deliberately unpinned) — run after every pin or plate change. `PLACES.md` is the regenerated term×plate coverage matrix (source of truth for pin data is each plate file, not the doc).
- **Wheel-zoom sensitivity:** `wheelPxPerZoomLevel` on both `AtlasMap` and `JourneyMap` is `15` (down from a sluggish default `40`) — lower is more sensitive/faster zoom per scroll tick.
- **Place deep links (`#atlas/<slug>/@<term>`):** place cards link into the Atlas focused on that place's pin. `findPin(term, preferSlug?)` in `src/data/plates/index.ts` resolves a term to `{slug, place}` (exact then case-insensitive; an explicit route slug outranks `PLATE_PRIORITY` = graecia → aegyptus → natoliae → palestinae → africae → rubri). `AtlasMap`'s `focusTerm` prop glides to the pin at `maxZoom - 1.5` (tuned by eye — `-2.5` is too wide to read Ortelius's own engraved toponym), reuses the crosshair announce-halo in view mode, and auto-opens the pin's popup via a term-keyed marker-ref registry. **Card click rule:** the 61 places whose art is only a shared full-plate map (every art key ends `-map`) open the Atlas on whole-card click; the 23 with real artwork keep gallery-first click plus a small "Map" button; Ocean (unpinned) is unchanged. Cards are therefore `div role="button"`, not `<button>` — a nested button would be invalid HTML. Popups gate "View artworks" on `hasArt` (real, non-map art), so map-only pins show just the name. The route builder `atlasHash(slug, term)` lives in the plate registry (not `App.tsx`) because the Atlas's own search emits the same links the cards do.
- **In-Atlas place search (view mode only):** `PlateSearch` in `AtlasMap.tsx` — a magnifier button in the modal header that expands to an input; below `sm` the title + plate `<select>` step aside while it's open. It searches **every** plate via `searchPins(q, preferSlug)` over `ALL_PINS` (both in `src/data/plates/index.ts`): substring match on term/label, deduped by term (the open plate wins, else `PLATE_PRIORITY`), prefix matches first and `noGloss` pins last, ≤8 results each labelled with its plate title. Picking one just sets `window.location.hash` to the deep link — same-plate re-fires `DeepLinkFocus` (no remount), cross-plate remounts via `key={slug}`. Edit mode keeps its separate `CalibrationFooter` pin *filter* (which filters the editable list rather than navigating) and renders no header search. **Gotcha:** the result list is NOT a DaisyUI `menu` — `.menu li > *` uses `grid-flow-col`, which drew the place name and plate name overlapping on one line; and the dropdown needs an explicit `z-[1000]` or the later-positioned map wrapper paints over it.
- **Gotcha — a popup open across a plate switch crashes the app.** `AtlasMap` remounts on `key={slug}`, tearing the popup down mid-flight; Leaflet's `DivOverlay.update()` then runs `_adjustPan` against a removed map (`Cannot read properties of null (reading 'layerPointToContainerPoint')`) and blanks the page. `DeepLinkFocus`'s effect cleanup closes the popup it opened. Rare when popups only opened on click; routine now that every deep link opens one. (Swapping a marker's *icon* is safe — the popup is its own map layer; only unmounting the marker kills it, which is the separate positional-key trap above.)
- **Gotcha — an occluded browser tab suspends `FitWhenReady`.** A backgrounded/hidden tab pauses the render loop, so the `ResizeObserver` never fires, `bounds` stays null, no tiles load and `focusTerm` never resolves — indistinguishable from a broken feature. When driving either map from an automation tool, force a paint (screenshot) between navigations.
- **Entry point / hash routing:** one `parseMapHash` parser in `App.tsx` serves both maps. `#atlas` / `#atlas/<slug>` open a plate (`#journey` / `#journey/<slug>` a journey), a trailing `/@<term>` focuses a pin (split off the RAW hash **before** lowercasing — pin terms are case- and punctuation-bearing — then resolved to the canonical term), and the **universal editing gate is `/edit` appended to any map route** — `#atlas/edit`, `#atlas/<slug>/edit`, `#journey/edit`, `#journey/<slug>/edit`. The pre-`/edit` spellings parse forever as legacy aliases (`#atlas-eyeball`, `#atlas/<slug>-eyeball`, `#journey/<slug>-eyeball`, and journey's original bare `#humaneyeball`); writers (AtlasMap's plate switcher) emit only the `/edit` form. No plate/journey slug may be named `edit`.
- **Pin calibration status (all plates now off their seed grids):** Graecia's 69 pins calibrated 2026-08-07; `rubri` pruned the same day from 100 → **31 pins** (every term Graecia's frame covers was deleted from it — rubri keeps only the Vlyssis-inset mythical/voyage isles, the Underworld rivers, Cyprus, and the Levantine/African/Asian pins) with its Arabia/Persia/India placeholders calibrated onto their printed labels (India is the one compromise — the plate prints no "India", so its pin sits mid-peninsula on "Mambari regnum"); `aegyptus`'s Egypt/Pharos calibrated the same day. The flagged Graecia pins were **reviewed + resolved 2026-08-07** (Argos↔Mycenae swapped onto their own engraved labels, Phylace moved to the Thessalian position, the four interpretive pins confirmed — see `graecia-draft/flags.md` "Review resolution"). **Not yet done:** no minimap-size tuning for mobile (fixed 132px width, same as JourneyMap); no drag-declutter pass for dense pin clusters at low zoom.

## Shared calibration UI (`src/CalibrationPanel.tsx`)
- Both editing modes (`#journey/edit`, `#atlas/edit`) render the same floating "Calibration" panel — title, hint text, a live-updating readonly JSON `dump` textarea, and a "Copy coordinates" button. Extracted into one shared component so this markup exists in exactly one place; the two maps' actual pin/route-dragging *logic* stays separate and is NOT shared, because the underlying coordinate math genuinely differs: `JourneyMap` converts via a plain `yx(x,y)=[H-y,x]` flip against its single `ImageOverlay`, while `AtlasMap` must go through the live map instance's `project`/`unproject` because its tiled `CRS.Simple` layer needs the `scale(zoom)=2^zoom` division a plain flip doesn't do (see the Atlas gotcha above). `JourneyMap` also has route-bend calibration (click-to-add/drag/dblclick-remove on the sea route) that Atlas has no equivalent of. Forcing those into one abstraction would either break Atlas's zoom correctness or bolt unused route machinery onto it, so only the panel *shell* is shared.
- **Portalled to `document.body`, not rendered as a child of `modal-box`, and it's load-bearing, not cosmetic.** DaisyUI's `.modal-box` sets `translate: 0; scale: 1` unconditionally (even at the identity values) — and per the CSS spec, ANY non-`none` value of `translate`/`scale`/`rotate`/`transform`/`filter` on an ancestor makes that ancestor the *containing block* for `position: fixed` descendants. A panel left inside `modal-box` with `position: fixed` would therefore only be draggable within the modal card's own box, not the actual browser viewport — `createPortal(..., document.body)` sidesteps that trap so the panel can genuinely be dragged anywhere on screen, including outside the visible map card.
- **Draggable via native `PointerEvent`s** (`pointerdown` on the "⠿ Calibration" header, `pointermove`/`pointerup` on `window`), position clamped to the viewport in `left`/`top` pixel state. **Gotcha for automated testing:** Chrome DevTools/`claude-in-chrome`'s synthetic `left_click_drag` only dispatches mouse events, not real `PointerEvent`s — a real user's mouse/trackpad fires both natively so dragging works fine for actual users, but automated drag-testing of this panel needs `dispatchEvent(new PointerEvent(...))` directly (via a `javascript_tool`-style page-context eval) to actually trigger the drag handlers.
- **Corner-clip gotcha (fixed, applied to both maps):** the map's rounded-corner clip must live on an INNER wrapper around just the `MapContainer` (`absolute inset-0 overflow-hidden rounded-box border ...`), not on the same outer `relative` container that also holds the floating panels. Putting `overflow-hidden rounded-box` on the shared outer container clips across the panel's own square top-left corner where the container's curve cuts into it — a visibly broken/jagged corner on the panel itself, easy to miss until someone screenshots it zoomed in.

### Mobile QA checklist — real device, not just simulator
Verified on iPhone SE **simulator** only (fixed the letterbox/cover-zoom bug there); NOT yet checked on a real phone. Test at `https://odysseygloss.vercel.app/#atlas`:
1. **Map fills the screen edge-to-edge in portrait**, no grey letterbox bands top/bottom (this was the bug fixed via the cover-zoom change above — confirm it holds on real hardware, not just the simulator).
2. **Pinch-to-zoom** — reasonably responsive, not sluggish. The `wheelPxPerZoomLevel` fix only affects mouse/trackpad scroll; touch pinch-zoom is a separate Leaflet code path that was **never touched or tested**.
3. **Drag-to-pan** with a finger.
4. **Tap a pin** (Egypt or Ethiopia) → popup appears → tap "View artworks" → lightbox opens on top of the map → close it → map should still be open underneath.
5. **The "×" close button** — on the simulator screenshot, the title "Atlas — the Red Sea Plate" wraps to two lines and the close button sits tight against the wrapped text. Confirm it's still comfortably tappable and not visually cramped/misaligned on a real screen.
6. **Minimap thumbnail** (bottom-left) — fixed-size (132px), never adjusted for mobile; check it isn't awkwardly large/intrusive on a small screen.
7. **Pin dragging in `#atlas/edit`** — drag a pin with a finger: it should follow the finger and commit new coords on release, and the map must NOT pan underneath. (Fixed 2026-07-16 by restoring native Leaflet drag; verified with emulated Chromium touch only — real iOS Safari unconfirmed.)
Same checklist applies to `JourneyMap` if it hasn't been device-tested recently — it shares the same wheel-zoom and cover-zoom code paths (both just changed).

## Image hosting (Cloudflare R2)
- All images and Atlas tiles are served from **Cloudflare R2** — account `484f994d4cda4a50fd47ae741cec8eb9` (`Nemocitizen@protonmail.com`'s account, a privacy alias the user controls on purpose), bucket `odyssey-assets`, public delivery via the `r2.dev` managed domain: `https://pub-b57180e24c9841f58854ecd1c164523a.r2.dev`. Migrated 2026-08-04 off Cloudinary (cloud `dhvvz91bh`) after that shared account was disabled for billing overage and permanently deleted — the account was shared across several unrelated local projects, and the overage was traced to this project's own bulk Atlas-tile upload (`747ffeb`/`da5c54b`, 5 days before the warning), not another project's traffic. `public/art/` is **git-ignored** (kept locally only as the upload master / for re-sync); it is NOT deployed. `public/hero.jpg` stays in the repo (LCP preload).
- **Object key scheme:** art masters at `art/<filename>.jpg` (mirrors `art.json`'s `file` field exactly, e.g. `file: "/art/achaeans-1.jpg"` → key `art/achaeans-1.jpg` → `assetUrl(a) = R2_BASE + a.file`); Atlas tiles at `atlas/<slug>/{z}/{row}/{col}.jpg` (see Atlas map section above); per-plate minimap thumbnails at `atlas/<slug>/thumb.jpg`.
- App delivers via `assetUrl(a)` in `App.tsx`: `${R2_BASE}${a.file}` — served as-is, no resize/format param. **R2 has no on-the-fly transform API** (unlike Cloudinary's `f_auto,q_auto,c_limit,w_N`), so there's no per-request AVIF/WebP negotiation or width capping anymore; this works because `public/art/` masters are already ImageMagick-recompressed to 1600px max / q82 / stripped at harvest time (see "Working with artwork" below), so serving the master directly is already close to optimal for both card covers and the lightbox. The `cld` field in `art.json` is now dead data (superseded by `file`-based URLs) — harmless, not worth a 574-entry cleanup pass.
- **Credentials:** `~/Documents/odyssey/.env.r2.local` (git-ignored via the `*.local` rule) — `CF_ACCOUNT_ID`, `CF_API_TOKEN` (Bearer token, account-wide `Workers R2 Storage: Edit`, used for bucket/domain admin calls), and `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_S3_ENDPOINT` (S3-compatible credentials, used for bulk object upload — see gotcha below).
- **Uploading (`scripts/upload_to_r2.py`):** walks `public/art/*.jpg` and `plates/<slug>/tiles/**/*.jpg` for all 6 plates and pushes everything via **boto3 against R2's S3-compatible endpoint** (`endpoint_url=R2_S3_ENDPOINT`, `region_name="auto"`), 16 concurrent workers. Re-running is idempotent (plain overwrite) — no resume/skip logic, just re-upload everything.
- **Gotcha — the generic Cloudflare REST API (`api.cloudflare.com`, Bearer token) is NOT viable for bulk upload.** It works fine for one-off admin calls (bucket create, enabling the public managed domain) but has an aggressive **account-wide** rate limit (~1200 req/5min ≈ 4 req/s) shared across *all* Cloudflare API usage on the account, not anything R2-specific — a first attempt at uploading all ~11,650 objects through it stalled at ~36% behind constant 429s, even with backoff/retry across three different concurrency strategies. **R2's separate S3-compatible endpoint has no such limit** (it's real object-storage infrastructure, not the shared control-plane API) and completed the same full upload with 0 failures and no throttling. Rule of thumb: REST API for admin/config calls, S3-compatible endpoint for any bulk object PUT/DELETE loop.
- **Swap an image in place (no `art.json` change):** overwrite the same R2 key (`s3.upload_file(..., ExtraArgs={"ContentType": "image/jpeg"})` with `overwrite` implicit — R2 PUTs always overwrite). Unlike Cloudinary there's no automatic CDN purge/versioning, so a browser that already cached the old bytes at that URL may keep serving them for a while — if that matters (as it did for the Journey map's base image, previously handled via Cloudinary's `/v<version>/` path segment), consider a cache-busting query param or a new key + updated reference instead of an in-place overwrite.
- **Orphan cleanup after removals:** removing an image from `art.json` leaves its R2 object unused. No scripted cleanup exists yet (the Cloudinary-era orphan-sweep script was cloud-specific) — for now, cross-reference `art.json`'s `file`/`cld` values against a bucket listing (`s3.list_objects_v2`) by hand if this comes up. Low priority: storage is cheap and nothing currently prunes.
- Shell here is **zsh** (no `mapfile`/bash array slicing) — use Python for montages/scripts.

## Deploy
`npm run build` then `vercel --prod --yes` (or just `git push` — auto-deploys). Lighthouse target was 100/100/100/100 (achieved locally; production perf varies with network).

## TODO / open items
0. **ACTIVE PLAN — place search → Atlas deep-links: repo-root `Plan.md`** (approved 2026-08-07; **Phases 0, A and B applied 2026-08-07** — deep links + in-Atlas search are live, next is Phase C, per-place cover crops, which is optional polish). Place cards/search deep-link into the Atlas focused on the place's pin (`#atlas/<slug>/@<term>`) instead of dead-ending in a gallery showing a generic map JPEG. Phased with per-phase model/effort suggestions: 0 = pin prereqs (folds in item 1 b/c), A = core wiring, B = in-Atlas search, C = cover crops, D = parked journey routing (would fold in item 3a). **Resume protocol: the user `/clear`s and prompts "next phase" → read `Plan.md`, run the first unchecked phase only.**
1. **Atlas pin follow-ups — ALL DONE, reminder retired** (see IN-FLIGHT note at top):
   (a) ~~spot-check the flagged Graecia pins against `graecia-draft/flags.md`~~ **DONE 2026-08-07** (review delegated to the assistant, re-run offline with marked-ring crops — see flags.md "Review resolution"): Chalcis/Crouni/Enipeus/Gyrae confirmed; Phylace → Thessalian (5080,4560); Argos↔Mycenae swapped onto their own engraved labels;
   (b) ~~delete the now-redundant Greek pins from `rubri.ts` and recalibrate its Arabia/Persia/India placeholders~~ **DONE 2026-08-07** (Plan.md Phase 0): 69 Greek terms deleted (rubri 100 → 31 pins), Arabia/Persia/India calibrated onto their printed labels;
   (c) ~~`aegyptus.ts`'s 2 pins (Egypt, Pharos) parked in "AEGYPTIVM MARE"~~ **DONE 2026-08-07** (Plan.md Phase 0): Egypt on the "AEGYPTVS INFERIOR" Delta label, Pharos on "Pharos colonia" island inside the Alexandria-nome inset.
2. **Mobile QA on a real device** for the Atlas map (and re-check Journey map) — see the checklist under "Atlas map" above. Only simulator-tested so far.
3. **Journey map — next steps:** (a) make the hero a small version of the map + add "Show on map" deep-links from the 14 journey-place cards; (b) optional mobile pin-declutter for the tight western cluster.
4. Optional Atlas polish: mobile-tune the minimap thumbnail size; a drag-declutter pass for dense pin clusters at low zoom.
5. A few minor-character entries still share scenes for lack of unique art.
6. Optional: masthead `ΟΔΥΣΣΕΙΑ` is redundant with "The Odyssey" (could become `ΟΜΗΡΟΥ` / be removed).
7. **Atlas Phase 6 (opted in, not started):** glossary entries for poem-text-only places that actually occur in Wilson's translation (doubtful: Arcadia/Attica/Thessaly/Laconia/Peloponnese/Boeotia — verify first; Arabia/India/Persia/Africa stay `noGloss` map-only pins). Shared-plate art path: def + zhName/zhPinyin/zhDef + `<slug>-map` art record reusing the plate it's pinned on (upload one 1600px copy of each new plate as a normal art asset).

**Done (context): Aug 2026 — migrated off Cloudinary to Cloudflare R2** (see "Image hosting" and "Atlas map" above for the full setup): Cloudinary's shared cross-project account was disabled for billing overage and permanently deleted; all 512 art masters and ~11,650 Atlas tiles (incl. regenerating `rubri`'s pyramid, whose tiles had never been persisted locally) re-uploaded to a new R2 bucket via boto3/S3-compatible endpoint (the plain REST API hit an account-wide rate limit at this volume); `App.tsx`, `AtlasMap.tsx`, all 6 plate configs, and the Journey map's own base-image reference rewired to R2 URLs; minimap thumbnails (previously a Cloudinary `c_crop`) pre-baked per plate since R2 has no live transform API.

every-image-appears-once dedup (Hayez/Pinturicchio/Kleophrades over-use removed; method: pull from the entry's Wikipedia article via `generator=images`, filter PD/CC); curated additions (June 2026: Burne-Jones *Wine of Circe*, Primaticcio *Ulysses and Penelope*, Esquiline Underworld panels, Sperlonga *Scylla Group*, an *L'Odissea* 1911 PD film frame → Cyclops — modern stills stay excluded); the caption/metadata fill, *Vlyssis Errores* fix, the interactive Journey map, and the tiled Atlas map of the full Red Sea plate with Egypt/Ethiopia pins. **July 2026:** added a third Atlas pin (Pharos, off Alexandria); widened the Atlas pin hit target to 40px + `cursor-grab`; extracted the two maps' floating calibration panel into shared `src/CalibrationPanel.tsx`; fixed a corner-clip bug where the map's `overflow-hidden rounded-box` wrapper sliced the panel's own top-left corner; made the calibration panel draggable anywhere in the viewport via a `document.body` portal; populated the atlas map with pins for the glossary places; improved calibration panel UX with top-right default on desktop and collapse toggle on mobile; restored native Leaflet pin dragging in `#atlas-eyeball` after a six-commit hand-rolled failure spiral; **generalized `JourneyMap` for multiple personages** (`src/data/journeys/` config registry, `#journey/<slug>(-eyeball)` routing — the "planned" TODO for this is done); **multi-plate Atlas expansion** (Phases 1–3, 5): registry refactor mirroring `src/data/journeys/`, 5 new Ortelius plates (Graecia, Aegyptus, Natoliae, Palestinae, Africae) tiled/uploaded/seeded via `scripts/make-plate.sh` + `scripts/upload_tiles.py`, `scripts/check-pins.ts` data guard, `DEFAULT_PLATE_SLUG` flipped to `"graecia"` — Phase 4's hands-on pin calibration landed 2026-08-07 and its flag review was resolved the same day (item 1 above).

## Sister project
`~/Documents/smell/web` → Vercel project `smellcombo`, live at https://smellcombo.vercel.app (Next.js; needs `vercel.json` with `{"framework":"nextjs"}`).
